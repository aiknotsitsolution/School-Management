{
  "name": "school-erp-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Scalable MERN microservices backend for School Management ERP System",
  "scripts": {
    "install:all": "npm install && npm install --prefix shared && npm install --prefix api-gateway && npm install --prefix services/auth-service && npm install --prefix services/student-service && npm install --prefix services/staff-service && npm install --prefix services/academic-service && npm install --prefix services/fee-service && npm install --prefix services/communication-service && npm install --prefix services/library-service && npm install --prefix services/facility-service",
    "start": "node monolith.js",
    "start:gateway": "npm --prefix api-gateway start",
    "dev:all": "node scripts/dev-all.js",
    "docker:up": "docker-compose up --build",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "db:clean": "node scripts/clean-db.js",
    "bootstrap:super-admin": "node scripts/bootstrap-super-admin.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^17.4.2",
    "imagekit": "^6.0.0",
    "mongoose": "^8.5.1",
    "multer": "^2.3.0",
    "nodemailer": "^10.0.3"
  }
}
